patchright==1.59.1
openpyxl==3.1.5
faster-whisper==1.2.1
requests==2.33.1
rapidfuzz==3.14.5
keyring==25.7.0
matplotlib==3.10.0
sv-ttk==2.6.1
pywin32==312
msal==1.37.0
pyinstaller==6.20.0
# Private shared analytics library — powers the Zenith > Reports sub-tab.
# Built from the analytics repo (`python scripts/build_wheel.py`) and vendored
# here so the build is self-contained. Lean, stdlib-only core (no duckdb/pandas).
./wheels/usba_reporting-0.1.5-py3-none-any.whl
