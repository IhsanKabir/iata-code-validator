patchright==1.59.1
openpyxl==3.1.5
faster-whisper==1.2.1
requests==2.33.1
rapidfuzz==3.14.5
keyring==25.7.0
matplotlib==3.10.0
sv-ttk==2.6.1
pywin32==312
msal==1.37.0
pyinstaller==6.20.0
# Instant Reports engine (Zenith > Reports > "Build from data"): same pins as
# the analytics machine so client-built numbers can never drift from published ones.
duckdb==1.5.2
pandas==3.0.2
pyarrow==24.0.0
# Private shared analytics library â€” powers the Zenith > Reports sub-tab
# (download list + the Instant Reports / mail-list windows). Built from the
# analytics repo (`python scripts/build_wheel.py`) and vendored here.
./wheels/usba_reporting-0.2.16-py3-none-any.whl
